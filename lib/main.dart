// Copyright (c) 2026, Owen Danke

// dart
import 'dart:core';

// flutter
import 'package:flutter/material.dart';
import 'package:flutter/services.dart';

// pub.dev packages
import 'package:google_fonts/google_fonts.dart';
import 'package:flutter_native_splash/flutter_native_splash.dart';

// httapp
import 'package:httapp/pages/main_navigation.dart';
import 'package:httapp/ui/theme.dart';
// httapp models

import 'package:httapp/services/catalog_service.dart';
// httapp services
import 'package:httapp/services/version_service.dart';
import 'package:httapp/services/text_theme_service.dart';
import 'package:httapp/services/map_controller_service.dart';
import 'package:httapp/services/firebase/app_check_config.dart';

// firebase api & options
import 'package:firebase_core/firebase_core.dart';
import 'firebase_options.dart';

/*
  Application entry point.

  Start-up sequence:
    1. Flutter engine bindings initialised.
    2. Firebase initialised.
    3. App Check activated.
    4. CatalogService.loadLocal() - reads local cache, no network.
    5. runApp() - UI renders immediately with whatever is complete locally.
    6. Post first-frame: CatalogService.sync() runs in background.
       As each entity finishes downloading, [CatalogService.entities]
       notifies and the UI updates automatically.
*/

/// Global singleton - widgets access entities via catalogService.entities.
late final CatalogService catalogService;

Future<void> main() async {
  //Bind (create) the Widget tree for the Flutter engine.
  final widgetsBinding = WidgetsFlutterBinding.ensureInitialized();

  // keep the splash screen alive until removed
  FlutterNativeSplash.preserve(widgetsBinding: widgetsBinding);

  // Initialize version service to get app version, name, package name, etc.
  VersionService().init();

  /// Initialize text theme service to manage text across the app during runtime
  TextThemeService().init(GoogleFonts.latoTextTheme());

  /// Initialize mapController service to handle the map controller
  MapControllerService().init();

  // Initialize Firebase, utilizing the firebase_options.dart file that is generated by FlutterFire CLI.
  await Firebase.initializeApp(options: DefaultFirebaseOptions.currentPlatform);

  // Activate App Check with automatic provider selection.
  try {
    await AppCheckConfig.activate();
  } catch(e) {
    debugPrint('[main] Exception during Appcheck activate: $e');
    rethrow;
  }

    // Load whatever is already on disk — no network calls.
    catalogService = CatalogService();
    await catalogService.loadLocal();


  // begin rendering the app widget, bootstrap the render tree
  runApp(const MainApp());

  // Remove splash once the first frame is ready.
  // Background sync starts after the first frame so the user sees the UI
  // without waiting for any downloads.
  WidgetsBinding.instance.addPostFrameCallback((_) {
    FlutterNativeSplash.remove();
    _startBackgroundSync();
  });
}

/// Kicks off the background sync. Errors are caught here so the app
/// continues running normally if sync fails (e.g. no network).
Future<void> _startBackgroundSync() async {
  try {
    await catalogService.sync();
  } catch (e) {
    debugPrint('[main] Background sync error: $e');
  }
}

class MainApp extends StatelessWidget {
  const MainApp({super.key});

  @override
  Widget build(BuildContext context) {
    final ColorScheme colorScheme = MaterialTheme.lightScheme();
    final TextTheme textTheme = GoogleFonts.latoTextTheme();

    // remove the spash screen manually
    FlutterNativeSplash.remove();

    return MaterialApp(
      title: 'Holcomb Tree Trail',
      home: const MainNavigation(), // use MainNavigation instead of HomePage

      // create custom theme here
      theme: ThemeData(
        // use lightScheme from theme.dart
        colorScheme: colorScheme, 
        // use Lato from Google Fonts
        textTheme: TextThemeService().textTheme,
        // app bar theme
        appBarTheme: AppBarThemeData(
          foregroundColor: colorScheme.onPrimary,
          backgroundColor: colorScheme.primary,
          systemOverlayStyle: SystemUiOverlayStyle.light,
        ),
        // bottom navigation bar theme
        navigationBarTheme: NavigationBarThemeData(
          backgroundColor: colorScheme.primary,
          indicatorColor: colorScheme.secondary,
          labelTextStyle: WidgetStateProperty.resolveWith<TextStyle>(
              (Set<WidgetState> states) => TextStyle(color: colorScheme.onPrimary),
            ),
          iconTheme: WidgetStateProperty.resolveWith<IconThemeData>(
              (Set<WidgetState> states) => states.contains(WidgetState.selected)
              ? IconThemeData(color: colorScheme.secondaryContainer)
              : IconThemeData(color: colorScheme.onPrimary),
            ),
          ),
        // create custom button themes
        elevatedButtonTheme: ElevatedButtonThemeData(
          style: ElevatedButton.styleFrom(
            // text/icons displayed on the button (white)
            foregroundColor: colorScheme.onPrimary,
            // color of the button (green)
            backgroundColor: colorScheme.primary,
            surfaceTintColor: Colors.white,
            // make the buttons 'larger', increase density
            visualDensity: VisualDensity(vertical: 3, horizontal: 2),
            textStyle: textTheme.bodyLarge,
            elevation: 4,
          ),
        ),
        filledButtonTheme: FilledButtonThemeData(
          style: FilledButton.styleFrom(
            // text/icons displayed on the button (white)
            foregroundColor: colorScheme.onPrimary,
            // color of the button (green)
            backgroundColor: colorScheme.primary,
          )
        ),
      ),
    );
  }
}